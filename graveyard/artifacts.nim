
      #let color = hsvToRgb(radialColorAt(x, y, 1024))
      #let color = hsvToRgb(radialColorAt(x, y, 255))
      #let color = hsvToRgb(radialColorAt(x, y, 257))
      #let color = hsvToRgb(radialColorAt(x, y, 2^33))
      #let color = hsvToRgb(radialColorAt(x, y, 100))
